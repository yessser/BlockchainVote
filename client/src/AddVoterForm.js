import React,{useEffect} from 'react'

const AddVoterForm = props => {
    const {inputs,inputTypes,state, handleInputChange, handleSubmit}=props
    useEffect(() => {
        
    }, )
    
    return (
        <div>
            XD
        </div>
    )
}

export default AddVoterForm
